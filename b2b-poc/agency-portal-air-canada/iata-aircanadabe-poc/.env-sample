# -----------------------------------------------------------------------------
# Local Application & Database Configuration
#
# These are default values for a local development environment.
# They are generally safe to keep in the example file.
# -----------------------------------------------------------------------------
NODE_ENV=local
APPLICATION_PORT=5012
APPLICATION_URL=http://localhost:5012

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DATABASE=aircanada_db
ROLLBACK_DATABASE=false

# For local development, you will need to provide the database user and password.
# Do NOT commit these credentials to version control.
POSTGRES_USERNAME=
POSTGRES_PASSWORD=


# -----------------------------------------------------------------------------
# External Service Credentials & IDs
#
# --- CRITICAL SECRETS ---
# The following values are highly sensitive. They include API keys and unique
# identifiers for external services. Leave them blank in this example file.
# The actual values should be stored ONLY in your local .env file.
# -----------------------------------------------------------------------------

# --- ORBIT Verifier Configs ---
# The base URL for the nborbit enterprise verifier API.
ENTERPRISE_VERIFIER_API_URL=
# The specific Line of Business (LOB) ID for the verifier.
ENTERPRISE_VERIFIER_API_LOB_ID=
# The API key for authenticating with the verifier service.
ENTERPRISE_API_API_KEY=
# The specific ID for the proof request definition to be used.
ENTERPRISE_PROOF_REQUEST_ID=


# --- IATA Trust Registry (TR) ---
# The base URL for the IATA Trust Registry API.
IATA_TR_BASE_URL=
# The API key for authenticating with the Trust Registry.
IATA_TR_API_KEY=


# --- Air Canada Keycloak IDP Config ---
# The base URL for the Air Canada Identity Provider API.
AIRCANADA_IDP_BASE_URL=
# The specific realm ID within the IDP to use.
AIRCANADA_REALM_ID=


# -----------------------------------------------------------------------------
# Application-Specific Configuration
# -----------------------------------------------------------------------------

# IATA code for the primary travel agency used in the application.
TRAVEL_AGENCY1_IATA_CODE=LAX

